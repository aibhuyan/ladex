"""Ladex — a bill of lading for AI.

Ladex records what AI is aboard a codebase — models, datasets, agent frameworks,
vector stores, inference APIs — captured at the moment the code is written, and emits a
CycloneDX ML-BOM with signed human attestations for the fields no scanner can derive.
"""

__version__ = "0.1.0"
